[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "homeready-florida"
version = "0.2.0"
description = "HomeReady Florida runnable MVP app and business logic"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
