[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "homeready-florida"
version = "0.1.0"
description = "MVP planning and automation core for HomeReady Florida"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
